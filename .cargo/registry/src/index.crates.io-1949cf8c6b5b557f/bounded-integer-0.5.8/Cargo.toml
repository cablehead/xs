# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
name = "bounded-integer"
version = "0.5.8"
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Bounded integers"
readme = "README.md"
keywords = [
    "bounded",
    "integer",
    "macro",
    "refinement",
]
license = "ISC"
repository = "https://github.com/Kestrer/bounded-integer"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = [
    "--cfg",
    "doc_cfg",
]

[lib]
name = "bounded_integer"
path = "src/lib.rs"

[[test]]
name = "hygiene"
path = "tests/hygiene.rs"

[dependencies.arbitrary1]
version = "1.0.2"
optional = true
package = "arbitrary"

[dependencies.bounded-integer-macro]
version = "=0.5.8"
optional = true

[dependencies.bytemuck1]
version = "1.7.2"
optional = true
package = "bytemuck"

[dependencies.num-traits02]
version = "0.2.14"
features = ["i128"]
optional = true
default-features = false
package = "num-traits"

[dependencies.serde1]
version = "1.0.124"
optional = true
default-features = false
package = "serde"

[dependencies.zerocopy]
version = "0.8.14"
features = ["derive"]
optional = true

[features]
__examples = [
    "macro",
    "bounded-integer-macro/generate_tests",
]
alloc = []
macro = ["bounded-integer-macro"]
serde = ["serde1"]
std = ["alloc"]
step_trait = []
types = []

[lints.rust.unexpected_cfgs]
level = "warn"
priority = 0
check-cfg = ["cfg(doc_cfg)"]
