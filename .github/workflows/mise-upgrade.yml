name: mise upgrade
# Generated by `mise run mise:repo:bootstrap`. Weekly tool + ref bump PR.
on:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  upgrade:
    uses: joeblew999/.github/.github/workflows/reusable-mise-upgrade.yml@v0.67.1
